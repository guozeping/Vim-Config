set nocompatbile               " 去除VI一致性，必须
filetype off                   " 必须

" 设置包括 vundle 和初始化相关的 runtimepath
set rtp +=~/.vim/bundle/Vundle.vim

call vundle#begin()

Plugin 'VundleVim/Vundle.vim'
" 所有插件的安装配置都必须在此范围内配置
{
	
}


" 所有插件需要在下面这行之前配置
call vundle#end()

